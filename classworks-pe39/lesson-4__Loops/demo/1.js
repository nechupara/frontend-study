/**
 * Цикл «while»
 *
 *
 * while (condition) {
 *   // код
 *   // также называемый "телом цикла"
 * }
 *
 */

let i = 2;

while (i < 10) { // выводит 0, затем 1, затем 2
	console.log( i );
	i = i+2;
}
