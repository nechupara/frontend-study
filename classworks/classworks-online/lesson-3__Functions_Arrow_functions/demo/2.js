/**
 *
 * Функция и переменные
 *
 * */

/*

function showMessage() {
	// let message = "Привет, я JavaScript!"; // локальная переменная
	var message = "Привет, я JavaScript!"; // локальная переменная
	// const message = "Привет, я JavaScript!"; // локальная переменная

	console.log( message );
}

showMessage(); // Привет, я JavaScript!

console.log( message ); // <-- будет ошибка, т.к. переменная видна только внутри функции
*/


//Внешние переменные
/*

let userName = 'Dan IT';

function showMessage() {
	let message = 'Привет, ' + userName;
	console.log(message);
}

showMessage(); // Привет, Dan IT*!/
*/
