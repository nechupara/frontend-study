/**
 * Это — объект, более сложная структура данных.
 * Объект используется для описания комплексные сущностей, например: пользователя.
 * Более подробно мы познакомимся с объектами в последующих частях курса.
 */
const user = {
  firstName: 'Walter',
  lastName: 'White',
  country: 'USA',
};

console.log(user);
