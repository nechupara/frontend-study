/**
 * Это — массив, ещё одна более сложная структура данных.
 * Массив используется для хранения списков данных.
 * Более подробно мы познакомимся с объектами в последующих частях курса.
 */
const items = ['apple', 'banana', 'orange'];

console.log(items);
